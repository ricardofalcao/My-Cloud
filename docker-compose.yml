# Use postgres/example user/password credentials
version: '3.1'

services:
  php:
    build: 
      context: ./docker
      dockerfile: php.Dockerfile
    restart: always
    volumes:
      - ./public:/var/www/html
      - ./:/var/www/
      - ./data:/data
    ports:
      - 8080:80
